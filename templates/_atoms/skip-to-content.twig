{% set skipId = skipId ?? 'main-content' %}

<a
    href="#{{ skipId }}"
    class="fixed z-50 block w-full -my-56 text-center bg-primary text-dark focus:my-0 focus:outline-hidden"
>
    <span class="inline-block px-8 py-2 mx-auto my-2">Skip to main content</span>
</a>
